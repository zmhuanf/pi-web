=====================================================
  pi-web 本地定制开发规范（避免与远端合并冲突）
=====================================================
适用范围：本仓库（zmhuanf/pi-web）相对上游 agegr/pi-web 的
本地定制。所有改动 AI 或人工修改前必须先读本文档。

-----------------------------------------------------
一、核心原则
-----------------------------------------------------
1. 本仓库 = 上游 agegr/pi-web + 本地定制。
   上游会持续更新，每次合并都可能与本地定制冲突。
2. 一切改法的唯一目标：合并冲突尽可能少。
3. 判定标准：git diff <上游> 的行数越少越好，
   尤其是「修改上游已有代码」的行数。

-----------------------------------------------------
二、代码放置规范（最重要）
-----------------------------------------------------
本地定制代码必须放进以下目录，禁止散落到上游文件：

    lib/zmhuanf/          服务端逻辑（估算、常量、工具函数）
    components/zmhuanf/   前端组件与 hooks

现有文件：
    lib/zmhuanf/preferences.ts          所有默认值常量（如 DEFAULT_TOOL_PRESET="full"）
    lib/zmhuanf/context-usage.ts        老对话用量估算
    components/zmhuanf/FollowButton.tsx 顶部栏跟随按钮

新增定制时：
    - 逻辑、组件、hook 一律放 zmhuanf/ 目录
    - 上游文件里只允许留「桥接」：一行 import + 一处调用/透传
    - 上游文件是零冲突的新增路径，改它们=主动制造冲突

-----------------------------------------------------
三、修改上游文件的红线
-----------------------------------------------------
禁止：
    1. 删除或重构上游已有代码（只许增，不许减）
    2. 修改上游的测试文件（*.test.mjs）
    3. 为让测试变绿而迁就实现
    4. 把本地常量硬编码在上游文件里（必须放进 lib/zmhuanf/ 并单独引用）

允许的最小改动（按优先级）：
    - import 一行
    - 函数体内加 1-2 行桥接调用
    - 组件 props 加一个可选参数并透传（尽量用 hook/组件吸收逻辑）

注意：
    - 测试失败不代表功能坏，上游基底本身就带失败用例
      （useAgentSession 4 个、MessageView 环境问题），不要为了
      修测试去动实现或改断言
    - 每次改完自查：git diff 上游 <文件>，确认没有多余删除

-----------------------------------------------------
四、现有定制清单（合并时人工重点检查）
-----------------------------------------------------
零冲突（新增文件，永留本地目录）：
    lib/zmhuanf/context-usage.ts      老对话用量估算
    lib/zmhuanf/scroll-anchor.ts      二次布局后贴底 + 锚点撑高保留
    lib/zmhuanf/thinking-inline.ts    最后一条思考内联判定
    components/zmhuanf/FollowButton.tsx
    bin/zmhuanf/ssh-launcher.js      SSH 启动包装（npm run start -- --ssh/--pass 摘参 →
                                     PI_WEB_SSH / PI_WEB_SSH_PASSWORD）

上游文件的桥接（合并冲突高发区，按风险排序；均为「import + 少行调用」）：
    hooks/useAgentSession.ts         跟随状态+toggleFollow、同构 layoutEffect、
                                     schedulePostLayoutBottomFix（~35 行）
    components/AppShell.tsx          跟随按钮 state/渲染 + 用量「已用/上限」格式（~30 行）
    components/ChatWindow.tsx        跟随状态桥接（props+useEffect）+ retainAnchorSpacer（~20 行）
    components/MessageView.tsx       编辑工具默认展开 2 行（用上游 isThinkingExpandedByDefault）
    lib/session-reader.ts            最后一条思考内联桥接（3 行）
    app/api/sessions/[id]/state/route.ts  老对话用量兑底
    lib/models-cache.ts              用量缓存失效挂钩
    lib/i18n/messages/en.ts zh-CN.ts 跟随按钮文案
    package.json                     端口 30142/Windows build 修复 + start 指向 ssh-launcher
                                     （保留上游 postinstall）

-----------------------------------------------------
五、本地定制功能清单（改坏前先确认需求）
-----------------------------------------------------
（本节仅列出本地独有的定制；默认工具预设已交回上游，见第十节）
1. 右上角用量显示「已用 / 上限」格式（AppShell）
2. 老对话也有用量信息（context-usage.ts + state 路由）
3. 跟随按钮（FollowButton + AppShell/ChatWindow/useAgentSession 桥接）
4. 思考防闪烁：最后一条思考内联（thinking-inline.ts）+
   流式 paint 前同步 + 折叠二次布局贴底（scroll-anchor）
5. 编辑工具默认展开（跟随上游思考展开设置）
6. 默认端口 30142 + Windows build 修复
7. SSH 远程模式：npm run start -- --ssh user@host[:/remote/path|:<port>] [--pass 密码]；
   启动包装摘参注入 PI_WEB_SSH / PI_WEB_SSH_PASSWORD，~/.pi/agent/extensions/ssh.ts
   在 session_start 读取实现 read/write/edit/bash 远程化（冒号后纯数字视为端口
   （非 / 开头），密码认证走 plink -pw（sshpass/askpass 在无 tty 下不可用），
   首次连接自动提取 hostkey 指纹缓存，改扩展时注意保持 pi CLI 兼容）

-----------------------------------------------------
六、合并上游的流程建议
-----------------------------------------------------
1. 小步合并：频繁 rebase 上游，不要攒大量改动后一次性合并
2. 合并前先读本清单第四节，知道哪些文件会撞
3. 冲突解决原则：zmhuanf/ 目录永远全选本地；
   上游文件的冲突逐 hunk 看，本地桥接尽量保留，
   上游新逻辑优先，桥接行重新放回
4. 合并后：跑 tsc --noEmit 确认类型，测试允许不全绿
5. 推送用 git push origin main（可能需要 --force 仅限
   明确要求时，正常情况用普通 push）
6. 不要 force 覆盖 origin/agegr-main：它保存着 2026-08 的 16 个旧定制提交
   （自研滚动/折叠/显示优化，作者 yefengfeng），与本地 agegr-main
   （纯上游快照分支）不是同一条线。本地 agegr-main 只用来 fetch 上游 main，
   不推送、不 checkout

-----------------------------------------------------
七、依赖安装红线（AI 别再弄坏本地环境）
-----------------------------------------------------
1. 本机终端默认 NODE_ENV=production。
   npm install 在此环境下会跳过全部 devDependencies
   （react-markdown、tailwindcss、typescript 等），
   表现为 tsc 报 Cannot find module react-markdown 之类。
2. 确需安装/恢复依赖时，只允许这一条命令：
       npm install --include=dev
   或用一次性环境变量：NODE_ENV=development npm install
   禁止只跑裸 npm install 后就宣称环境已恢复。
3. 合并上游与依赖无关时，禁止顺手执行 npm install /
   npm ci / 清理 node_modules。依赖环境只归本地人工维护。
4. node_modules 缺包时先问人工，不要自作主张重装整个
   依赖树。Windows 下 npm 清理遇 EPERM 会留下半删目录，
   进一步破坏 node_modules。

-----------------------------------------------------
九、v0.9.1 合并记录（2026/09/21，上游 1eb5e66）
-----------------------------------------------------
本次结果：
    - agegr-main：8366762（v0.9.1）→ 1eb5e66，快进 45 提交
    - main：9ab313e（Merge branch 'agegr-main'）
    - 上游实际新增 36 提交（112 文件 +4386/-811），无新依赖
    - 冲突仅 3 文件：hooks/useAgentSession.ts、components/ChatWindow.tsx、
      components/MessageView.tsx
    - 自动合并：AppShell.tsx、ChatInput.tsx、session-reader.ts、models-cache.ts、
      i18n、package.json、agent/[id]/route.ts、events 与 state 路由
    - 合并后 git diff upstream/main = 24 文件（下方清单）

冲突解法（下次同类直接照抄）：
    - hooks 状态：本地旧的 useState<ToolPreset>(DEFAULT_TOOL_PRESET) 已不存在
      （v0.9.2 移除，见第十节），现在沿用上游 CONFIGURED_TOOL_PRESET；
      上游新增 state（newSessionThinkingLevel 等）全采纳；上游已删本地
      thinkingLevel state，勿再恢复
    - hooks 滚动回调：本地 setFollowState 行与上游 shouldShowScrollToLatest 行共存
    - ChatWindow：本地 follow props 与上游 onOpenFile(filePath, page?) 合并，
      解构写成 following, toggleFollow, showScrollToBottom,
    - MessageView 工具卡：useState(defaultExpanded || isToolCallExpanded(id))，
      本地默认展开只做初值，上游 toggleExpanded 持久化逻辑整体保留

固定流程（逐条执行）：
    1. git fetch https://github.com/agegr/pi-web.git main:agegr-main
    2. git switch main 然后 git merge --no-ff agegr-main -m "Merge branch 'agegr-main'"
    3. 冲突按上面解法处理，zmhuanf/ 目录永远全选本地
       （.git/config 已开 rerere.enabled=true，重复冲突会自动套用上次解法）
    4. node_modules/.bin/tsc --noEmit
    5. git diff --stat 上游基线 对照第十节清单，多出的文件=桥接被覆盖或本地误改
    6. git push origin main

遗留不一致（本地既存，不是某次合并引入，改前先问人工）：
    - components/AppShell.mobile-toolbar.test.mjs 改了上游断言
      （mobileContextText = desktopContextText），与第三节「禁止改上游测试」冲突
    - package-lock.json 用 npmmirror 源、devDeps 版本范围高于上游

-----------------------------------------------------
十、v0.9.2 合并记录（2026/09/23，上游 040fadd / tag v0.9.2）
-----------------------------------------------------
本次结果：
    - agegr-main：1eb5e66 → 040fadd，快进 15 提交
    - main：aa1d3a3（Merge branch 'agegr-main'）
    - 上游 101 文件 +7729/-1554；pi SDK 0.85.1 → 0.87.0（本次必须装依赖）
    - 冲突 3 文件：components/ChatInput.tsx、hooks/useAgentSession.ts、
      lib/tool-preset-preference.ts；AppShell/ChatWindow/session-reader/i18n/
      package.json/lock/events 路由均为自动合并

冲突主因：上游 #700 新增 configured 预设（默认值，未显式选择就跟随
settings.json 的 defaultTools），正面撞上本地「默认 full 且强制对齐后端」。

人工决定（2026/09/23）：移除该本地定制，回归上游 configured：
    - 删除 lib/zmhuanf/preferences.ts 与全部 DEFAULT_TOOL_PRESET 引用
    - useAgentSession 的 state 初值/loadContext/loadTools 恢复上游写法
      （顺带消除 loadTools 用旧 toolPreset 强制对齐、把用户选择弹回去的 bug）
    - ChatInput、tool-preset-preference 恢复 040fadd 原样
    - 删除 prompt/bash 附带的 toolNames 与 events 路由的冷启动 toolNames 参数
    - 默认 full 改由 ~/.pi/agent/settings.json 提供：
      defaultTools = ["read","powershell","edit","write","grep","find","ls"]
      （新机器要重新配这条，否则新会话只有四个工具）

固定流程：
    1. git fetch https://github.com/agegr/pi-web.git main:agegr-main
    2. git switch main 然后 git merge --no-ff agegr-main -m "Merge branch 'agegr-main'"
    3. 冲突按上面解法，zmhuanf/ 目录永远全选本地（rerere 已开）
    4. 上游动了 package.json 依赖才 npm install --include=dev
    5. node_modules/.bin/tsc --noEmit
    6. git diff --stat 040fadd 对照下方清单，多出的文件=桥接被覆盖或本地误改
    7. git push origin main

git diff --stat 040fadd 预期清单（22 文件）：
    AI-DEV-RULES.txt、app/api/agent/[id]/route.ts、
    app/api/sessions/[id]/state/route.ts、bin/pi-web-options.js、
    bin/zmhuanf/ssh-launcher.js、components/AppShell.mobile-toolbar.test.mjs、
    components/AppShell.tsx、components/ChatWindow.tsx、components/MessageView.tsx、
    components/zmhuanf/FollowButton.tsx、hooks/useAgentSession.ts、
    lib/i18n/messages/en.ts、lib/i18n/messages/zh-CN.ts、lib/models-cache.ts、
    lib/powershell-settings.ts、lib/session-reader.ts、
    lib/zmhuanf/context-usage.ts、lib/zmhuanf/scroll-anchor.ts、
    lib/zmhuanf/ssh-session.ts、lib/zmhuanf/thinking-inline.ts、
    package-lock.json、package.json

合并后遗留：
    - npm install 后残留 node_modules/@earendil-works/.pi-coding-agent-1nx88yLL
      （Windows EPERM 半删目录，待清理）
